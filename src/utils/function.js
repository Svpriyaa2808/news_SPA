export const getImageUrl = image => {
    return new URL(`../assets/${image}`,import.meta.url).href;
}

export const sportsImageUrl = image => {
    return new URL(`../assets/${image}`,import.meta.url).href;
}
































































